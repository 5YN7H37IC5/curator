# Patches

This directory will contain patch files and upgrade scripts that apply modifications to the existing codebase.

Examples:
- `upgrade_dataset_schema.patch`: Changes for new schema compatibility
- `ci_finetune_workflow.sh`: Script to incorporate dataset generation into CI